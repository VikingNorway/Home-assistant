# ios devices
# put in configuration.yaml
ios:
  push:
    categories:
      - name: Coffeemaker
        identifier: 'coffeemaker'
        actions:
          - identifier: 'COFFEEMAKEROFF'
            title: 'Switch coffeemaker off'
            activationMode: 'background'
            authenticationRequired: yes
            destructive: yes
            behavior: 'default'

    